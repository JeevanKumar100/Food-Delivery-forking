apiVersion: v1
kind: Service
metadata:
  name: food-delivery-backend-service
spec:
  selector:
    app: food-delivery-backend
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
